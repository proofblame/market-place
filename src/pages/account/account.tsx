import React from "react";
import style from "./account.module.scss";
import { Container } from "../../components";

export const Account = () => {
  return (
    <Container>
      <section className={style.account}>Account</section>
    </Container>
  );
};
